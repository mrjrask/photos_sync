<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.jason.photoslocalsync</string>

    <key>ProgramArguments</key>
    <array>
        <string>/bin/bash</string>
        <string>/Users/jason/photos_sync/sync_photos_local.sh</string>
    </array>

    <!-- Runs once a day at 3:15 AM (offset from the NAS sync's 3:00 AM so
         the two don't compete for Photos library access if both are
         installed on the same Mac). -->
    <key>StartCalendarInterval</key>
    <dict>
        <key>Hour</key>
        <integer>3</integer>
        <key>Minute</key>
        <integer>15</integer>
    </dict>

    <!-- Also runs once whenever this agent (re)loads, e.g. at login,
         so you're not stuck waiting until 3:15 AM to catch up. -->
    <key>RunAtLoad</key>
    <true/>

    <key>StandardOutPath</key>
    <string>/Users/jason/Library/Logs/photos-local-sync.out.log</string>
    <key>StandardErrorPath</key>
    <string>/Users/jason/Library/Logs/photos-local-sync.err.log</string>

    <key>ProcessType</key>
    <string>Background</string>
</dict>
</plist>
